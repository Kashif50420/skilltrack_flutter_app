name: silk_track
description: Silk Track Learning Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_secure_storage: ^9.1.1
  url_launcher: ^6.2.1
  
  # NEW PACKAGES ADDED:
  video_player: ^2.8.2
  chewie: ^1.5.0
  screenshot: ^1.3.0
  share_plus: ^7.2.1
  # Optional: Add if you need more features
  # flutter_svg: ^2.0.9
  # carousel_slider: ^4.2.1
  # image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/data/programs.json